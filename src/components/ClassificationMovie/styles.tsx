import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        //alignSelf: 'center',
        padding: 8,
        borderRadius: 5,
    },

    text: {
        color: '#fff',
        fontSize: 15
    }
});